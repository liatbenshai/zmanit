# זמנית - שדרוג מלא 🚀

## מה חדש בגרסה הזו

### 1. 🎨 דשבורד חדש ומרהיב!
- ציטוט מוטיבציוני יומי
- 🔥 סטריק - ספירת ימים רצופים
- טבעת התקדמות ויזואלית
- "משימה בוערת" - הכי דחוף עכשיו
- עיצוב צבעוני וחי

### 2. 🔄 גרירת משימות לשינוי סדר
- גררי משימה למעלה או למטה
- הסדר נשמר אוטומטית
- 3 פסים (☰) בצד = ידית גרירה

### 3. 📅 העברת משימות
בכל משימה יש כפתור 📅:
- מחר
- עוד יומיים
- עוד שבוע

### 4. 📊 מערכת למידה
- לומדת מכל משימה שמסיימים
- מציעה הערכות מדויקות יותר
- טאב "למידה" 🧠 בהגדרות

### 5. ✅ מודל משוב בסיום
- מציג הערכה מול ביצוע
- אפשרות לתקן את הזמן

### 6. 🏠 משימות בית (17:00-21:00)
- כביסה 🧺, קניות 🛒, ניקיון 🧹, בישולים 🍳

### 7. ⚙️ סוגי משימות מותאמים אישית
הגדרות > סוגי משימות

---

## 📦 קבצים בחבילה

```
src/
├── config/
│   └── taskTypes.js              ← סוגים מותאמים
├── pages/
│   └── Settings.jsx              ← טאבים חדשים
├── components/
│   ├── Dashboard/
│   │   └── SmartDashboard.jsx    ← דשבורד חדש! 🎨
│   ├── DailyView/
│   │   ├── DailyTaskCard.jsx     ← העברה + משוב
│   │   ├── SimpleTaskForm.jsx    ← הצעות חכמות
│   │   └── DraggableTaskList.jsx ← גרירה! 🔄
│   └── Notifications/
│       └── NotificationChecker.jsx
└── utils/
    ├── taskLearning.js           ← מערכת למידה
    └── taskOrder.js              ← שמירת סדר גרירה
```

---

## 🔧 התקנה

### שלב 1: החלפת קבצים (6 קבצים)
- `src/config/taskTypes.js`
- `src/pages/Settings.jsx`
- `src/components/Dashboard/SmartDashboard.jsx`
- `src/components/DailyView/DailyTaskCard.jsx`
- `src/components/DailyView/SimpleTaskForm.jsx`
- `src/components/Notifications/NotificationChecker.jsx`

### שלב 2: הוספת קבצים חדשים (3 קבצים)
- `src/utils/taskLearning.js`
- `src/utils/taskOrder.js`
- `src/components/DailyView/DraggableTaskList.jsx`

### שלב 3: שילוב גרירה ב-DailyView.jsx

ב-DailyView.jsx, הוסיפי את ה-import:
```jsx
import DraggableTaskList from './DraggableTaskList';
import { sortTasksByOrder, saveTaskOrder } from '../../utils/taskOrder';
```

החליפי את הרינדור של המשימות מ:
```jsx
{upcomingBlocks.map((block, index) => (
  <DailyTaskCard ... />
))}
```

ל:
```jsx
<DraggableTaskList
  tasks={upcomingBlocks}
  dateISO={getDateISO(selectedDate)}
  onEdit={handleEditTask}
  onUpdate={loadTasks}
  onOrderChange={(newOrder) => {
    saveTaskOrder(getDateISO(selectedDate), newOrder);
    loadTasks();
  }}
/>
```

---

## 💡 שימוש

### גרירת משימות
1. רחפי על משימה - תראי 3 פסים (☰) בצד
2. לחצי וגררי למעלה או למטה
3. שחררי במיקום הרצוי
4. הסדר נשמר אוטומטית!

### הוספת סוג משימה חדש
הגדרות ⚙️ > סוגי משימות 📋 > + הוסף

### צפייה בסטטיסטיקות
הגדרות ⚙️ > למידה 🧠

---

## 🎉 ליהנות!
