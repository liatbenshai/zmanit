-- ==============================================
-- הוספת שדה הערות למשימות
-- ==============================================

-- הוספת עמודת notes לטבלת tasks
ALTER TABLE public.tasks ADD COLUMN IF NOT EXISTS notes TEXT;

-- הערה: שדה זה מאפשר למשתמש להוסיף הערות חופשיות למשימה
-- למשל: תוצאות, סיכום, נקודות לזכור וכו'
